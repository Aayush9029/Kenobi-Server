on:
  release:
    types: [published]
name: Release
jobs:
  pypi:
    name: Release to PyPI
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Deploy package
      uses: remorses/pypi@master
      with:
        setupfile: ./kenobi/setup.py
        username: ${{ secrets.pypi_username }}
        password: ${{ secrets.pypi_password }}

